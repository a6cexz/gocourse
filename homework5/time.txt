1. Возможно стоит указать в методичке более явно, что формат даты и времени при печати отталкивается от определенной шаблонной даты
Mon Jan 2 15:04:05 -0700 MST 2006

2. Также полезная информация о том как хранить даты в базе данных (UTC) и как показывать даты пользователям (учитывать их location)

3. Пример на разбор даты из строки - тоже может оказаться полезным